---
"dumbridge": patch
---

Decompose the Fallow-ranked complexity hotspots (served-root host file reads, wire frame encoding, manifest entry validation, the incremental frame reader) into named cohesive steps with behavior unchanged.
