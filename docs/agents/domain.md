# Domain docs

dumbridge has one domain context.

Before changing behavior, read the root `CONTEXT.md` and relevant decisions in `docs/adr/`. Use glossary terms in code, tests, issues, and documentation. Surface conflicts with an ADR instead of silently overriding it.
