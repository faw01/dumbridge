# Dumbridge

Dumbridge gives a disposable cloud coding agent temporary, live, read-only access to one local directory.

The v1 command surface is `serve`, `run`, and `pull`. Those commands are being implemented behind the CLI scaffold; `dumbridge --help` and `dumbridge --version` are available now.

Requires Bun 1.3.14 or newer.
